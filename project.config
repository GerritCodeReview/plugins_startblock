[access]
	inheritFrom = All-Projects
[submit]
	action = inherit
[project]
	description = Frontend plugin that lets owners set submit requirements on a change. For now, this has no effect on the backend.
